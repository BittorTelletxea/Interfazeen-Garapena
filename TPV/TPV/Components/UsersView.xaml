<UserControl x:Class="TPV.Components.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             Background="White">

	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Text="Gestión de Usuarios"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

		<DataGrid x:Name="dgUsers"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  Margin="0,10,0,10">
			<DataGrid.Columns>
				<DataGridTextColumn Header="ID" Binding="{Binding UserId}" IsReadOnly="True" Width="70"/>
				<DataGridTextColumn Header="Username" Binding="{Binding Username, Mode=TwoWay}" Width="150"/>
				<DataGridComboBoxColumn Header="Role"
                                        SelectedItemBinding="{Binding Role, Mode=TwoWay}"
                                        Width="100">
					<DataGridComboBoxColumn.ItemsSource>
						<x:Array Type="{x:Type sys:String}">
							<sys:String>Admin</sys:String>
							<sys:String>User</sys:String>
						</x:Array>
					</DataGridComboBoxColumn.ItemsSource>
				</DataGridComboBoxColumn>
			</DataGrid.Columns>
		</DataGrid>

		<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
			<Button Content="Añadir" Width="100" Margin="5" Click="Add_Click"/>
			<Button Content="Editar" Width="100" Margin="5" Click="Edit_Click"/>
			<Button Content="Eliminar" Width="100" Margin="5" Click="Delete_Click"/>
			<Button Content="Refrescar" Width="100" Margin="5" Click="Refresh_Click"/>
		</StackPanel>
	</Grid>
</UserControl>
