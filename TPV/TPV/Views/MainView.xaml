<Window x:Class="TPV.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:component="clr-namespace:TPV.Components"
        Title="TPV - Main View" Height="700" Width="1100">

    <Grid Margin="0,0,0,-80">

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10">
            <Button x:Name="btnTPV"
                    Content="TPV"
                    Width="120"
                    Height="40"
                    Margin="0,0,10,0"
                    Background="#3498DB"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="btnTPV_Click"/>

            <Button x:Name="btnErreserbak"
                    Content="Erreserbak"
                    Width="120"
                    Height="40"
                    Background="#BDC3C7"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="btnErreserbak_Click"/>
            <Button Content="Tiket-a inprimatu" Width="150" Margin="5" Background="#E67E22" Foreground="White" Click="BtnImprimir_Click"/>

        </StackPanel>

        <Grid x:Name="gridTPV" Margin="0,60,0,0">
            <component:CalculatorControl x:Name="calculadora" HorizontalAlignment="Left" VerticalAlignment="Bottom"  Width="390" Height="350"/>
            <component:ProductsGridControl x:Name="productsGrid" HorizontalAlignment="Right" VerticalAlignment="Bottom" Height="350" Width="700"/>
            <component:ProductSelecter x:Name="productSelecter" HorizontalAlignment="Right" VerticalAlignment="Top" Height="330" Width="700"/>
            <component:PrezioTotala x:Name="prezioTotala" HorizontalAlignment="Left" VerticalAlignment="Top" Height="320" Width="390"/>
        </Grid>

        <Grid x:Name="gridErreserbak" Margin="0,60,0,0" Visibility="Collapsed">
            <component:ErreserbakIkusi x:Name="erreserbakControl" />
        </Grid>

    </Grid>
</Window>
